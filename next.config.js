/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pixabay.com'], 
      },
      reactStrictMode: false,  // turn to false

}

module.exports = nextConfig
